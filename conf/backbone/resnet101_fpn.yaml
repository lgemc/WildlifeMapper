# ResNet-101 with FPN backbone configuration
backbone_type: "resnet101_fpn"
backbone_out_channels: 256

backbone_config:
  variant: "resnet101"
  pretrained: true
  out_channels: 256
  fpn_levels: [1, 2, 3, 4]